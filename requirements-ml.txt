# Local ML experiments only (drop-type classifier, docs/proposals/PLAN_drop_type_classifier.md).
# NEVER merged into requirements.txt — Render must not install torch.
pandas>=2.0
pyarrow>=14
scikit-learn>=1.4
lightgbm>=4.3
sentence-transformers>=3.0
matplotlib>=3.8
